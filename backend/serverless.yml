service: express-api

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs20.x
  region: eu-north-1
  environment:
    NODE_ENV: production

functions:
  app:
    handler: handler.handler
    events:
      - httpApi: '*'

plugins:
  - serverless-offline
